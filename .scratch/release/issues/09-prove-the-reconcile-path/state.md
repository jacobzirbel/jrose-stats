# 09 — Take one run through a second log to live, with a real second person

**Status:** ready-for-human
**Branch:** _not started_ — suggested `09-prove-the-reconcile-path`

**What to build:** one run carried end-to-end through the two-logger path with an actual second
human — second slot claimed, logged blind, submitted, differences reconciled, run reaching the
live state and its detail page becoming publicly visible.

**Blocked by:** 03 (redeem an invite), 04 (slot-2 gate switch)

Runs are already logged (`08` done), so this needs only the invite path and the gate switch.

This is the one thing that must happen *before* the slot-2 gate is closed. Once it's closed, the
reconcile path stops running in production entirely, and its first real-world exercise would
otherwise happen months later across many videos at once. Anything this turns up is worth
knowing now, while it's one run and two cooperating people.

- [ ] A second person, on their own account, claims and completes a second log on a run JZ logged
- [ ] Where the two logs disagree, the disagreement surfaces and is reconciled
- [ ] The run reaches the live state and its detail page is visible to a signed-out visitor
- [ ] Any friction or breakage found is written down before the gate is closed
- [ ] The slot-2 gate is closed afterwards

## Notes
