# SQLite database file (WAL mode). Created on first run.
DATABASE_URL=./data/app.db

# HTTP port for the Hono server
PORT=3000

# Seed 1B-2 (video/run linking). Leave unset to seed reference data only.
YOUTUBE_API_KEY=
YOUTUBE_PLAYLIST_ID=
